#include <stdio.h>
/**
 * main - entry point
 *
 * Return: Return 0 if successful, otherwise return EOF
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
